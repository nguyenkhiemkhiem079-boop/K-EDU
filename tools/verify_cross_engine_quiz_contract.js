const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),vm=require('node:vm'),vact=require('../js/vact');
const ctx={window:{}};ctx.window=ctx;vm.createContext(ctx);vm.runInContext(fs.readFileSync(path.resolve(__dirname,'..','js/quizContract.js'),'utf8'),ctx);const C=ctx.QuizContract;
for(const [method,expected,type] of [['generateMini100',100,'vact_mini_100'],['generateFull120',120,'vact_full_120']]){const exam=vact.VACTExamGenerator[method]({difficulty:'balanced',seed:'contract-'+method});const dto=vact.VACTExamGenerator.formatExamAsQuiz(exam,{title:method,timeLimitMinutes:method==='generateFull120'?150:90});const q=C.normalizeQuiz(dto);assert.equal(q.sourceType,type);assert.equal(q.totalQuestions,expected);assert.equal(q.answerKeys.length,expected);assert.equal(q.document.kind,'generated_html');assert.ok(C.validateQuiz(q).valid);}
for(const raw of [{id:'math',title:'Math',subject:'toan',timeLimit:45,answerKeys:[{}],examHtml:'<p>Câu 1</p>'},{id:'khtn',title:'Physics',subject:'khtn',discipline:'vat_ly',timeLimit:45,answerKeys:[{}],examHtml:'<p>Câu 1</p>'}]){const q=C.normalizeQuiz(raw);assert.ok(C.validateQuiz(q).valid);assert.equal(q.document.kind,'generated_html');}
console.log('Cross-engine canonical quiz contract passed.');
